# generated per-run, consumed by the workflow's commit step — never committed
data/commit_msg.txt
__pycache__/
*.pyc

# daily derived cache — regenerated each run; each pick embeds its driving numbers in its thesis
data/metrics.json
data/news.json
